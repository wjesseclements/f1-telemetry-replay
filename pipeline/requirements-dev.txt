# Test dependencies — what CI installs, and all that `pytest` needs.
#
# Deliberately does NOT include fastf1 or pandas. `replay_transform.py` imports numpy
# and the standard library only, so the offline guarantee (CLAUDE.md: app, tests and
# CI run fully offline) is structural: the network-dependent library is not even
# importable in the environment the tests run in.
pytest>=8
pytest-cov>=5
numpy>=2.5.1
