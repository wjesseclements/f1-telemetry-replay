<!-- PR title is the squash commit. Use Conventional Commits: feat: / fix: / chore: / refactor: / test: -->

## What & why

<!-- One or two sentences. Link the PLAN.md slice this closes. -->

## Verify

<!-- The slice's Verify step — how you confirmed it actually works. -->

- [ ] `typecheck` + `lint` + `test` + `build` green locally (CI re-checks)
- [ ] Exercised it on the Vercel preview deployment (feel/behaviour, not just the diff)
- [ ] No CLAUDE.md rule violations (clock-in-ref · `cars[]` is an array · engine stays pure · O(1) sample lookup)

## Notes

<!-- Trade-offs, follow-ups (push to the PLAN.md backlog), screenshots if UI changed. -->
