export default function App() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2 bg-bg text-txt">
      <h1 className="font-mono text-sm font-bold tracking-[0.2em] text-txt">
        TELEMETRY REPLAY
      </h1>
      <p className="text-sm text-dim">Scaffold ready — slices to come.</p>
    </main>
  );
}
